run_id: run_004
experiment_name: exp_001_cifar10_subset_study
seed: 0
description: 'Mild overfitting: smaller subset, light regularization'
dataset:
  train_size: 3000
  val_size: 1000
  augmentation: true
model:
  dropout: 0.1
training:
  optimizer: adam
  lr: 0.001
  lr_scheduler: cosine
  batch_size: 64
  weight_decay: 0.0
  epochs: 20
  gradient_clip_norm: null
  early_stop_on_divergence: true
  divergence_loss_threshold: 100.0
