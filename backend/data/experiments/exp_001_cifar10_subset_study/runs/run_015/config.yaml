run_id: run_015
experiment_name: exp_001_cifar10_subset_study
seed: 0
description: "Healthy baseline with one changed hyperparameter (batch size) \u2014\
  \ for testing config-diff and similarity tools against run_001"
dataset:
  train_size: 5000
  val_size: 1000
  augmentation: true
model:
  dropout: 0.3
training:
  optimizer: adam
  lr: 0.001
  lr_scheduler: cosine
  batch_size: 128
  weight_decay: 0.0001
  epochs: 20
  gradient_clip_norm: null
  early_stop_on_divergence: true
  divergence_loss_threshold: 100.0
